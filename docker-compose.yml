version: '3.8'

services:
  ros:
    image: osrf/ros:humble-desktop
    dns:
      - 8.8.8.8
      - 8.8.4.4
    volumes:
      - .:/ros_ws
      - ~/.ssh:/root/.ssh:ro
    working_dir: /ros_ws
    stdin_open: true
    tty: true
    command: /bin/bash
    environment:
      - ROS_DOMAIN_ID=1